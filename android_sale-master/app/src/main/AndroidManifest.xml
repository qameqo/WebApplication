<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abc.sale">

    <!-- API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- networkSecurityConfig is also for API. (see below) -->

    <!-- Camera -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Google Map and GPS Location -->
    <uses-feature android:name="android.hardware.location.gps" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Google Map -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <activity
            android:name=".LoginActivity"
            android:label="Login">
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EmployeeIndexActivity"
            android:label="รายการพนักงาน"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".EmployeeViewActivity"
            android:label="รายละเอียดพนักงาน"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EmployeeAddActivity"
            android:label="เพิ่มข้อมูลพนักงาน"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".EmployeeUpdateActivity"
            android:label="แก้ไขข้อมูลพนักงาน"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EmployeeDeleteActivity"
            android:label="ลบข้อมูลพนักงาน"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CameraActivity"
            android:label="Camera"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".QrActivity"
            android:label="QR Code"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MapsActivity"
            android:label="Google Map"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProductBarActivity"
            android:label="Bar Chart"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".ProductPieActivity"
            android:label="Pie Chart"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".ProductLineActivity"
            android:label="Line Chart"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>